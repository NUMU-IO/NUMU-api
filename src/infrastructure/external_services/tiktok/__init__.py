"""TikTok Events API integration (Pixel + server-side conversions).

Sibling of ``external_services/meta``. Houses the PII hashing helper and
(inline in the Celery task) the Events API v1.3 HTTP client.
"""
