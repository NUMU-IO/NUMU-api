# Version Guard Report — Skipped

No dependency sources found (no lockfile, package.json, or tech stack decision record).

NUMU-api is a Python/FastAPI project; the version-guard skill checks npm sources only. Python dependency pinning is managed via `requirements.txt` / `pyproject.toml` and is out of scope for this hook.
